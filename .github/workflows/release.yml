name: release
on:
  workflow_dispatch:
    inputs:
      semver:
        description: The semver to use
        required: true
        default: patch
        type: choice
        options:
          - patch
          - minor
          - major
      release:
        type: boolean
        default: false

jobs:
  run-file:
    uses: ./.github/workflows/ci.yml
    with:
      environment: hey there
