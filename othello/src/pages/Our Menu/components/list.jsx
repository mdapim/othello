import React from 'react';

export default function List({ title, description, price }) {
  <span>
    {console.log(title)}
    <>
      <h1> {title}</h1>
      <p> {description}</p>
    </>
    <p>{price}</p>
  </span>;
}
