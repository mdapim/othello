name: my ci push

on:
  workflow_call:

jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Write echo
        run: |
          echo "this is echo working" ${{ inputs.release }}
